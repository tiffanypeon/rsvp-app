<h1> Hi <%= @invite.guest_name %> ! </h1> 

<h2> RSVP For: <%= @invite.event.name %> </h2> 

<%= form_for(@invite) do |f| %> 
  <%= f.label :status %> 
  <%= f.check_box :status %> 

  <p> <%= f.submit %> </p> 
<% end %> 